<!DOCTYPE html>
<html>
<head>
  <title>Book A Retreat.com - Rent a Retreat Location or Conference Center</title>
  <%= stylesheet_link_tag 'application', 'app', 'style', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <%= yield :head %>
  <script>
    setTimeout(function() {
      $('.notice, .alert').fadeOut();
    }, 3000 );
  </script>
</head>
<body>

<%= render "layouts/admin_nav" if current_user.try(:admin?)%>

<%if !!notice%><p class="notice btn-success"><%= notice %></p><%end%>
<%if !!alert%><p class="alert btn-warning"><%= alert %></p><%end%>

<%= yield %>


</body>
</html>
